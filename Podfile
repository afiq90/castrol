# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'Castrol' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Castrol
    pod 'Firebase/Core'
    pod 'Firebase/Auth'

  target 'CastrolTests' do
    inherit! :search_paths
    # Pods for testing
    
  end

  target 'CastrolUITests' do
    inherit! :search_paths
    # Pods for testing
    
  end

end
